<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">
    <data>
        <variable
            name="playlist"
            type="com.aguosoft.app.videocamp.model.VMPlaylist"/>
    </data>
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_margin="5dp">

        <ImageView
            android:id="@+id/imageView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:adjustViewBounds="true"
            android:minHeight="120dp"
            android:scaleType="fitCenter"
            app:imageUrl="@{playlist.imagePath}"
            tools:src="@drawable/ic_channel_header_default"/>
        <FrameLayout
            android:layout_alignRight="@id/imageView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignEnd="@id/imageView"
            android:layout_alignTop="@id/imageView"
            android:layout_alignBottom="@id/imageView"
            android:minWidth="80dp"
            android:background="#a0000000">
            <TextView
                android:text="@{String.valueOf(playlist.videoCount)}"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignParentRight="true"
                android:layout_gravity="center"
                android:drawablePadding="10dp"
                android:drawableBottom="@drawable/ic_menu_s"
                android:textColor="@color/textSecondary_Chen_Dark"
                android:textSize="18sp"
                tools:text="1000"/>
        </FrameLayout>
        <TextView
            android:id="@+id/tv_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/imageView"
            android:textColor="?android:textColorPrimary"
            android:maxLines="2"
            android:text="@{playlist.name}"
            tools:text="TitleTitleTitleTitleTitleTitleTitleTitleTitleTitle"/>

        <TextView
            android:id="@+id/tv_subtitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="12sp"
            android:maxLines="1"
            android:layout_below="@id/tv_title"
            android:text="@{playlist.updatedAt}"
            tools:text="publishedAt"/>

    </RelativeLayout>
</layout>
